.left
  .left__top
    .left__top__name
      = current_user.name
    .left__top__btn
      =link_to new_group_path, class: "left__top__btn top__btn--left" do
        = icon('fas', 'pen-square', class: "top__btn--icon")
      =link_to edit_user_path(current_user), class: "left__top__btn top__btn--right" do
        = icon('fas', 'cog', class: "top__btn--icon")
  .left__bottom
    - current_user.groups.each do |group|
      .group
        = link_to group_messages_path(group) do
          .left__bottom--title
            = group.name
          .left__bottom--log
            メッセージはまだありません。